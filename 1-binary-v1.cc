#include <iostream>
#include <fstream>
#include <vector>
#include "1-binary-v1.hh"
using namespace std;


vector<int> dic, text;


void mergeSort(vector<int>& dic) {
    int mig = dic.size()/2;
    vector<int> dicf(mig);
    vector<int> dicl(dic.size()-mig);
}





void binaryV1(vector<int>& dict, vector<int>& entr) {
    dic = dict;
    text = entr;
}

int main() {
    binaryV1();
}
